$ErrorActionPreference='Stop'; $r=Split-Path $PSScriptRoot -Parent; $x=Get-Content "$r\Cosmic-Realms-main\Server-src\wServer\realm\Parties.cs" -Raw; foreach($s in 'Max=10','Create','Invite','Accept','Leave','Leader','PartyState'){if($x -notmatch [regex]::Escape($s)){throw "Missing $s"}}; if($x -match 'Mark|Forge|Potion'){throw 'Forbidden system change'}; 'PASS: account-backed party membership, invitations, cap, and leader checks are present.'
